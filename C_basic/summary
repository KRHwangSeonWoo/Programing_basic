20230926
0. 표준입출력
#포맷스트링 담을 수 있는 수보다 크게 담으면 버퍼오버플로 발생
하지만 그 수의 맞는 포맷스트링을 통해 출력하면 정상 출력 O
그 수의 맞지 않는 포맷스트링을 통해 출력하면 -1 반환

1. 변수와 자료형
#const
const를 사용하면 상수화가 되고 수정이 불가능함
const int a = 1;
a = 2; -> 에러

#정적 지역변수
정적 지역변수는 매개변수로 사용 X 
함수 내에 static int a = 0을 선언하고 return a++을 하면 해당 함수 호출 시마다
1이 더해짐 (함수가 끝나도)
#전역 변수
함수 밖에서 생성한 변수이며 파일 내에서 자유롭게 사용이 가능하다.
하지만 static을 붙이면 마찬가지로 매개변수로 사용이 불가능하고, 
다른 파일에서 불러들일수 없다 (extern...등등)

#열거형
enum을 이용하여 변수명에 숫자를 1,2,3,4,....차례대로 넣을 수 있다.
enum number {
	one, two, tree, four = 9999
};
enum number n;
	n = one;
	printf("%d\n", n);
	//0


2. 전처리기 지시자
#include
표준 헤더 파일은 <>로 일반 헤더 (custom)은 ""로 감싸서 선언

#define
컴파일러가 코드를 처리하기 전에 미리 정의된 값을 지정 -> static이랑 똑같다고 보면 되지만
#define -> 외부에서 가져올수있음
static -> 외부에서 가져올 수 없음

